# SOP: การมอบหมายงานให้ Agent (Agent Task Delegation)

**เวอร์ชัน:** 1.0  
**วันที่สร้าง:** 31 มกราคม 2026  
**ผู้สร้าง:** BMAD Master  
**เหตุผล:** แก้ไขปัญหา Agent เงียบหาย/ไม่เสร็จงานเมื่อมอบหมายงานใหญ่

---

## 🎯 วัตถุประสงค์

SOP นี้กำหนดมาตรฐานการมอบหมายงานให้ Sub-agent เพื่อ:
- ป้องกัน Agent เงียบหายหรือทำงานไม่เสร็จ
- ติดตามความคืบหน้าได้ตลอดเวลา
- มี Fallback plan เมื่อเกิดปัญหา
- ได้รับผลลัพธ์ที่ตรงตามความต้องการ

---

## ⚠️ ปัญหาที่พบ (และแก้ไข)

| ปัญหา | ผลกระทบ | วิธีแก้ไขใน SOP |
|-------|---------|----------------|
| Agent เงียบหาย | ไม่รู้ว่าทำงานอยู่หรือไม่ | Checkpoint ทุก 5-10 นาที |
| งานใหญ่เกินไป | Agent ติดค้างหรือล้มเหลว | แบ่งเป็น Micro-tasks |
| ไม่มี output | หาที่อยู่ไฟล์ไม่เจอ | บังคับบันทึกไฟล์ทันที |
| ไม่รู้ progress | รอนานแล้วไม่รู้ว่าเสร็จหรือยัง | Report status ทุก task |

---

## 📋 ขั้นตอนการมอบหมายงาน (Step-by-Step)

### Step 1: วิเคราะห์งานก่อนมอบหมาย

**ตรวจสอบรายการนี้:**
- [ ] งานนี้ต้องใช้เวลานานกี่นาที/ชั่วโมง?
- [ ] มีกี่ส่วนย่อยที่ต้องทำ?
- [ ] มี dependencies ระหว่างส่วนหรือไม่?
- [ ] ถ้า Agent ล้มเหลว จะทำยังไง?

**เกณฑ์การตัดสินใจ:**
```
ถ้างาน <= 10 นาที และไม่ซับซ้อน → ส่งเป็น Single Task
ถ้างาน > 10 นาที หรือซับซ้อน → แบ่งเป็น Micro-tasks
```

---

### Step 2: แบ่งงานเป็น Micro-tasks (ถ้าจำเป็น)

**หลักการแบ่งงาน:**
1. **แบ่งตามลำดับเวลา:** Task แรกต้องเสร็จก่อน Task ถัดไป
2. **แบ่งตามส่วนประกอบ:** แต่ละส่วนทำงานได้อิสระ
3. **แบ่งตาม deliverable:** แต่ละ Task มี output ชัดเจน

**ตัวอย่างการแบ่งงาน:**

```
❌ งานใหญ่ (ผิด):
"ออกแบบสถาปัตยกรรมระบบทั้งหมด"

✅ งานย่อย (ถูกต้อง):
Task 1/5: C4 Level 1-2 (Context + Container) - 10 นาที
Task 2/5: C4 Level 3 (Components) - 10 นาที
Task 3/5: Database Schema - 10 นาที
Task 4/5: API Design - 10 นาที
Task 5/5: Code Examples - 10 นาที
```

---

### Step 3: สร้าง Task Template ที่ถูกต้อง

**Template สำหรับแต่ละ Task:**

```markdown
[TASK X/Y] ชื่องานที่เฉพาะเจาะจง

**งาน:** อธิบายสั้นๆ ว่าต้องทำอะไร

**ข้อมูลที่จำเป็น:**
- บริบทโครงการ
- ข้อกำหนดเฉพาะ
- ตัวอย่าง/References

**ต้องสร้าง:**
1. รายการที่ 1
2. รายการที่ 2

**Output:**
- บันทึกที่: `path/ที่/ชัดเจน/filename.md`
- รูปแบบ: Markdown, PlantUML, etc.

**Checkpoint:**
- ต้องเสร็จใน: X นาที
- บันทึกไฟล์ทันทีที่เสร็จ
- Report กลับมาว่า: ✅ เสร็จ หรือ ❌ ไม่สำเร็จ + เหตุผล

**Fallback:**
ถ้าทำไม่สำเร็จ ให้: [ระบุว่าจะทำยังไง]
```

---

### Step 4: Spawn Agent พร้อม Monitoring

**คำสั่งที่ใช้:**
```bash
sessions_spawn(
    task="[TASK X/Y] ชื่องาน\n\n[ใส่ Template จาก Step 3]",
    runTimeoutSeconds=600  # 10 นาที = 600 วินาที
)
```

**ระยะเวลาที่เหมาะสม:**
- งานง่าย (read, search): 60-120 วินาที
- งานปานกลาง (write, edit): 180-300 วินาที
- งานซับซ้อน (design, code): 600 วินาที (10 นาที)

---

### Step 5: ติดตามความคืบหน้า (Monitoring)

**ตรวจสอบทุก 2-3 นาที:**
```bash
sessions_history(sessionKey, limit=20)
```

**สิ่งที่ต้องดู:**
1. Agent กำลังทำงานอยู่หรือไม่ (มี tool calls ใหม่ไหม)
2. เจอ error หรือไม่
3. มี output หรือยัง

**เกณฑ์การตัดสินใจ:**
```
ถ้า Agent ทำงานปกติ → รอต่อ
ถ้า Agent เงียบ > 5 นาที → ถามสถานะ
ถ้า Agent error → ไป Fallback
ถ้า Agent เสร็จ → ไป Task ถัดไป
```

---

### Step 6: Validate Output

**ตรวจสอบว่า Task เสร็จจริง:**
- [ ] ไฟล์ถูกสร้างจริงที่ path ที่ระบุ
- [ ] เนื้อหาครบถ้วนตามที่ขอ
- [ ] ภาษา/รูปแบบถูกต้อง

**คำสั่งตรวจสอบ:**
```bash
ls -la path/to/output/
cat path/to/output/file.md | head -50
```

---

### Step 7: Fallback (ถ้า Agent ล้มเหลว)

**ทางเลือกเมื่อ Agent ทำไม่สำเร็จ:**

| สถานการณ์ | การแก้ไข |
|-----------|----------|
| Agent เงียบหาย | Spawn ใหม่ หรือ Master ทำเอง |
| Agent error | ส่งต่อให้ Master แก้ไข |
| Output ไม่ครบ | ส่งให้ Agent ทำต่อ หรือ Master ทำต่อ |
| หมดเวลา | ขยายเวลา หรือแบ่งงานย่อยอีก |

---

## 📝 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: งานง่าย (Single Task)

**สถานการณ์:** อ่านไฟล์แล้วสรุป

```python
sessions_spawn(
    task="อ่านไฟล์ PRD แล้วบอกว่ามีกี่หน้า\n\nไฟล์: ~/project/prd.md",
    runTimeoutSeconds=60  # 1 นาทีพอ
)
```

---

### ตัวอย่างที่ 2: งานซับซ้อน (Micro-tasks)

**สถานการณ์:** ออกแบบสถาปัตยกรรมระบบ

```python
# Task 1/5
sessions_spawn(
    task="""[TASK 1/5] C4 Level 1-2
    
**งาน:** สร้าง C4 Context + Container Diagrams

**Output:** ~/project/architecture-part1.md

**Checkpoint:**
- เวลา: 10 นาที
- บันทึกทันทีที่เสร็จ
- Report: ✅ หรือ ❌""",
    runTimeoutSeconds=600
)

# รอให้ Task 1 เสร็จ แล้วค่อยส่ง Task 2
# Task 2/5
sessions_spawn(
    task="""[TASK 2/5] C4 Level 3
    
**งาน:** สร้าง Component Diagram

**Output:** ~/project/architecture-part2.md

**Checkpoint:**
- เวลา: 10 นาที
- บันทึกทันทีที่เสร็จ
- Report: ✅ หรือ ❌""",
    runTimeoutSeconds=600
)
```

---

## ✅ Checklist ก่อนมอบหมายงาน

**ต้องตรวจสอบทุกครั้ง:**

- [ ] งานถูกแบ่งเป็น Micro-tasks หรือไม่ (ถ้าใหญ่)
- [ ] มี Output path ที่ชัดเจน
- [ ] มีเวลาจำกัดที่เหมาะสม
- [ ] มี Checkpoint/Report กลับมา
- [ ] มี Fallback plan
- [ ] ไม่มีคำสั่งคลุมเครือ (ambiguous)

---

## 🤖 Autonomous Mode (การทำงานอัตโนมัติ)

**เมื่อไหร่ใช้:** เมื่องานถูกแบ่งเป็น Micro-tasks ที่ชัดเจนแล้ว

**หลักการทำงาน:**
1. **ไม่ถามระหว่างทาง** - ทำ Task ต่อเนื่องไปเรื่อยๆ
2. **รายงานเฉพาะ:**
   - ✅ เมื่อเสร็จทั้งหมด
   - ❌ เมื่อเจอปัญหาที่ไปต่อไม่ได้ (blocking issue)
3. **Spawn ต่อทันที** - Task เสร็จแล้ว spawn Task ถัดไปเลย ไม่ต้องรอถาม

**ตัวอย่าง:**
```
Master: ส่ง Task 1/5 → Agent ทำ → เสร็จ → Master ส่ง Task 2/5 ทันที
(ไม่ถามผู้ใช้ระหว่างทาง)

รายงานผู้ใช้ตอนจบ:
"เสร็จทั้ง 5 Tasks:
- Task 1: ✅ 
- Task 2: ✅
- Task 3: ❌ (เจอปัญหา X) 
- Task 4: ✅
- Task 5: ✅"
```

**ข้อดี:**
- เร็วกว่า (ไม่ต้องรอถApprove ทุกขั้น)
- ลดการกระโดด context ไปมา
- Master ควบคุม workflow เองได้เต็มที่

**ข้อควรระวัง:**
- ต้องแบ่งงานดีมากๆ ก่อนใช้ Autonomous Mode
- ต้องมี Fallback ชัดเจน
- ผู้ใช้ต้องพร้อมรับผลลัพธ์แบบ end-to-end

---

## ⚡ Parallel Execution (การทำงานขนาน)

**เมื่อไหร่ใช้:** เมื่อ Tasks ไม่มี dependencies ต่อกัน

**หลักการ:**
```
ถ้า Task A และ Task B ไม่ต้องรอกัน → ส่งพร้อมกันเลย
ถ้า Task C ต้องรอ Task A → รอ A เสร็จค่อยส่ง C
```

**ตัวอย่าง:**
```
✅ ทำขนานได้:
Task 4: API Design     ──┐
Task 5: Code Examples ──┘ ส่งพร้อมกัน (ไม่ต้องรอกัน)

❌ ต้องทำลำดับ:
Task 1: Read PRD    → Task 2: Analyze requirements
(ต้องรอ 1 เสร็จก่อน)
```

**ข้อดี:**
- เร็วขึ้นมาก (ลดเวลารอ)
- ใช้ resources ได้เต็มที่
- เหมาะกับงานอิสระ (independent tasks)

**การตัดสินใจ:**
| สถานการณ์ | วิธีการ |
|-----------|---------|
| Tasks อิสระกัน | Parallel (ส่งพร้อมกัน) |
| Tasks มี dependency | Sequential (รอลำดับ) |
| ไม่แน่ใจ | Sequential (ปลอดภัยกว่า) |

**ตัวอย่างการใช้งานจริง:**
```python
# Tasks 4 และ 5 ไม่มี dependency → ส่งพร้อมกัน
sessions_spawn(task="Task 4: API Design...")
sessions_spawn(task="Task 5: Code Examples...")
# ทำงานขนานกัน รอผลพร้อมกัน
```

---

## 🚨 สิ่งที่ห้ามทำ (Anti-patterns)

❌ **ห้ามมอบหมายแบบนี้:**
```
"ทำสถาปัตยกรรมให้หน่อย"
→ ไม่ชัดเจน Agent ไม่รู้จะทำอะไร

"ทำให้เสร็จภายในวันนี้"
→ เวลานานเกินไป ไม่มี checkpoint

"ทำตาม PRD แล้วสร้างเอกสาร"
→ ไม่ระบุว่าอยู่ที่ไหน ชื่ออะไร
```

✅ **ต้องมอบหมายแบบนี้:**
```
"[TASK 1/3] อ่าน PRD แล้วสรุป requirements\n\nไฟล์ PRD: ~/project/prd.md\n\nOutput: ~/project/requirements-summary.md\n\nCheckpoint:\n- เวลา: 10 นาที\n- บันทึกทันทีที่เสร็จ\n- Report กลับมา"
```

---

## 📚 References

- Agent Task: Nurse AI Architecture (31 Jan 2026) - ตัวอย่างที่สำเร็จ
- BMAD Methodology - หลักการแบ่งงาน
- OpenClaw Documentation - sessions_spawn

---

**Last Updated:** 2026-01-31  
**Version:** 1.0  
**Owner:** BMAD Master Agent
