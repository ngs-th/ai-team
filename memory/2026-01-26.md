# 26/1/2026

- ปรับระบบรายงาน "มุมสบาย" ให้ใช้ template แบบ ASCII table ใน code block เพื่อให้คอลัมน์ไม่เพี้ยนใน Telegram
- กำหนดรายงานเป็น ENGLISH ONLY + เรียงหมวด A→Z + มีเงื่อนไขพิเศษ: หมวด 3-แลกเงิน (CashExchange) ไม่นับเป็นรายได้ (exclude จาก Income/Net; แสดงเป็น Excluded)
- บันทึก template ไว้ที่: /Users/ngs/clawd/templates/moomsabaii_daily_finance_report_template.txt
- Boat แนะนำให้ใช้ subagent ทำงานแบบขนาน และเลือกใช้โมเดล/เครื่องมือให้เหมาะงาน
- ศึกษา "Party Mode" ใน obsidian-vault/CLAUDE.md: สั่งด้วย "Party Mode: <topic>" → spawn 3-5 subagents (roles) ทำ parallel deep think → cross-talk → consensus

## Session 2 (Evening)

### Moomsabaii
- ทดสอบ daily_finance_report.py สำหรับวันที่ 25/1/2026 → report ออกมาสวยงาม รวม Income 673, Net +673 (excl. CashExchange 100), MTD Net -12,748.30

### NGS
- วางแผน Payment Voucher workflow ใน payables_jan_2026.md:
  - เพิ่ม section "Voucher Plan" พร้อมลิงก์ SOP-Payment-Voucher.md
  - ระบุ payees ที่ยังขาดใน ngs_finance.db (พนักงาน, ภาษี, เจ้าหนี้)
  - แนะนำการจัดกลุ่ม PV 5 ชุด (salary/tax/employee debt/former staff debt/subscriptions)
- ย้ายงาน PV ไปหัวข้อ "🧾 PV (On Hold)" ใน ngs_kanban เพราะข้อมูลยังไม่ครบ

### File Cleanup (obsidian-vault)
- ลบไฟล์ bug claude-mem: CLAUDE.md ที่อยู่ผิดที่ (4 ไฟล์)
- Commit กลุ่ม A: PDFs (QT/IV/RC nurse-ai) + TOR drafts (EduAdmin, eLearning)
- Commit databases: moomsabaii.db, ngs_finance.db
- Commit exported CSVs for accounting: customers/invoices/quotations/receipts/revenue_summary
- git status สะอาดแล้ว

### System
- Node warning: `/usr/local/bin/node` ต่ำกว่า 22 แต่ daemon ใช้ v24.11.1 อยู่แล้ว → ข้ามไปก่อน
- Claude model issue investigation:
  - Default model = openai-codex/gpt-5.2
  - anthropic:claude-cli (OAuth) สถานะ "expiring" (~27 ชม.)
  - anthropic:default (token) สถานะ "static" (ใช้ได้)
  - วิธีใช้ Claude: `/model opus@anthropic:default` หรือตั้ง auth order ให้ prefer token
  - เริ่มทดสอบ auth แต่ session ถูก compact ก่อนได้ผล
  - ทดสอบสำเร็จ: ตั้ง auth order ให้ใช้ token profile ก่อน + เปลี่ยน default เป็น opus
- LINE channel: ต้องมี public HTTPS endpoint → เพิ่มใน backlog ไว้ก่อน
- **Boat ส่ง IV20260005 + IV20260006 (CSTP Email) เรียบร้อยแล้ว** — ย้ายเข้า Done

### Formatting Rule (IMPORTANT - repeated reminder)
- **Tables = ENGLISH ONLY** เพราะภาษาไทยมีสระ/วรรณยุกต์ทำให้ spacing เพี้ยน
- บันทึกใน TOOLS.md แล้ว
